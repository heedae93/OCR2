# ============================================================================
#  BBOCR Backend Dependencies
# ============================================================================
#
#  주의: paddlepaddle-gpu는 CUDA 버전에 따라 별도 설치해야 합니다.
#        이 파일에는 포함하지 않았으므로 README.md의 설치 가이드를 참고하세요.
#
# ============================================================================

# ── FastAPI 서버 ──
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# ── PaddleOCR + PaddleX (레이아웃 감지) ──
paddleocr>=3.1.0
paddlex>=3.1.0

# ── Table Transformer (Microsoft DETR 기반 표 인식) ──
#   paddlepaddle 와 torch 는 같은 환경에서 공존 가능합니다.
#   GPU 환경에서는 torch+cuda 빌드를 별도 설치하면 GPU 가속이 됩니다.
transformers>=4.30.0
timm>=0.9.0
torch>=2.0.0

# ── 이미지 처리 ──
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# ── PDF 처리 ──
PyMuPDF>=1.23.0
pdf2image>=1.16.0
reportlab>=4.0.0
PyPDF2>=3.0.0

# ── 데이터 모델 ──
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ── 유틸리티 ──
PyYAML>=6.0
aiofiles>=23.0.0
python-dateutil>=2.8.0

# ── DB ──
sqlalchemy>=2.0.0
psycopg2-binary==2.9.11

# ── 기타 ──
scikit-learn>=1.3.0

# ── LangChain (paddlex 내부 의존성 - 1.x 버전은 호환 안됨) ──
langchain>=0.3.0,<1.0.0
langchain-community>=0.3.0

# ── Celery + Redis (OCR 작업 큐) ──
celery[redis]>=5.3.0
redis>=5.0.0
pymysql==1.1.2

# ── KoBERT NER ──
sentencepiece>=0.1.96
duckduckgo-search>=6.0.0
